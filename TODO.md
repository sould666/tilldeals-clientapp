# TODO

- Add a native Windows sensor backend using LibreHardwareMonitor or Core Temp-compatible access for CPU core/package temperatures. Keep `Not reported` when firmware or drivers expose no valid sensor.
- Populate `src/renderer/diagnosis-outcomes.js` with more symptom checklists over time. Only `Monitor migocze` is filled in as a worked example; unlisted symptoms fall back to the generic step list.